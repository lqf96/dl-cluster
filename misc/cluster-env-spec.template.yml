channels:
  - conda-forge
  - defaults
dependencies:
  - python={dl_python_version}
  - jupyterlab
  - jupyter-archive
  - tensorboard
  - pip
  - pip:
    - determined={dl_det_version}
    - tensorboard-plugin-profile
